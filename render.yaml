services:
  - type: web
    name: python-tutor
    runtime: node
    plan: free
    buildCommand: npm install --production
    startCommand: node dist/index.cjs
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
